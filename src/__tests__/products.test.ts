
describe('Products Service', () =>{
    test('should add new product', () => {
        expect.assertions(1)
    })

    test('should check to see if product already exists', () => {
        expect.assertions(1)
    })

    test('should update product', () => {
        expect.assertions(1)
    })

    test('should delete product', () => {
        expect.assertions(1)
    })

    test('should get all products', () => {
        expect.assertions(1)
    })

    test('should get a product', () => {
        expect.assertions(1)
    })

    test('should not get anything if product does not exist',() => {
        expect.assertions(1)
    })
})